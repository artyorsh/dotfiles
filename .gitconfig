[core]
	excludesfile = ~/.gitignore_global
[user]
	name = Artur Y
	username = artyorsh
        email = artur@artyorsh.me
[alias]
	conflicts = diff --name-only --diff-filter=U
[credential]
	helper = osxkeychain
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
